export const metadata = {
    title: "McDonald's x Slack",
    description: '',
    icons: {
        icon: "/ifttt-applet/favicon.ico",
    }
  }
  
  export default function Layout({ children }) {
    return <div>{children}</div>
  }