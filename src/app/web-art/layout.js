export const metadata = {
    title: "Art of the Web",
    description: '',
    icons: {
        icon: "/web-art-portfolio/favicon.ico",
    }
  }
  
  export default function Layout({ children }) {
    return <div>{children}</div>
  }